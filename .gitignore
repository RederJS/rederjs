node_modules/
dist/
coverage/
*.db
*.db-shm
*.db-wal
*.env
.DS_Store
*.log

# Per-workspace reder shim pointer — contains a session token, not for git.
.mcp.json
# TypeScript incremental build cache
*.tsbuildinfo
# Local design-exploration scratch (see docs/design/ if anything graduates)
design/
# Superpowers workflow artifacts (specs graduate to docs/design/; plans stay local)
docs/superpowers/
# Per-user Claude Code state (settings, scheduled tasks, worktrees) — never for git.
.claude/
