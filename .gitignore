node_modules/
dist/
coverage/
*.db
*.db-shm
*.db-wal
*.env
.DS_Store
*.log

# Per-workspace reder shim pointer — contains a session token, not for git.
.mcp.json
# TypeScript incremental build cache
*.tsbuildinfo
# Local design-exploration scratch (see docs/design/ if anything graduates)
design/
